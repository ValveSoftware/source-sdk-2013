// APM minimal autoexec — universal setup only

// Cheats required for FCVAR_CHEAT cvars (dev environment)
sv_cheats 1

// Universal console settings
developer 1
con_timestamp 1

// Keep default logging simple; detailed NB debug lives in nbdebug.cfg
con_logfile "nbdebuglogs/nextbot.debug.txt"

echo "[APM] Autoexec (minimal) loaded. For NB tools: exec nbdebug.cfg"
