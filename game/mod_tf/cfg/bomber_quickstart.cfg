// Frog Bomber — use from any listen-server game (host only).
// Typical flow: launch mod_tf -> join/create any match -> exec bomber_quickstart
// (Same as ff_play bomber: mode cfg + changelevel itemtest + auto arena setup.)

exec ff_aliases.cfg
ff_play bomber

echo "Bomber quickstart: switching to itemtest sky arena..."
echo "  After load: join RED or BLU, pick Scout. Stuck? bm_fix  then  kill"
